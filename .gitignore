# Claude Code / local-only
.claude/
memory/

# node (news tracker deps — installed only in CI runner)
node_modules/
package.json
package-lock.json

# OS junk
Thumbs.db
desktop.ini
.DS_Store

# personal / unrelated local files — keep out of the public site repo
smr-live-caption/
triso_excel/
*.zip
*.exe
폴더생성기.*
__pycache__/
*.pyc
